{
    "version": 2,
    "buildCommand": "npm run build",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.js"
        }
    ],
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                    "dist/**"
                ]
            }
        }
    ]
}